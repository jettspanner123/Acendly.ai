{
  "name": "gand-phad-ai",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:secrets": "bash -c 'source ./scripts/dev-secrets.sh && exec $SHELL'",
    "dev:open": "bash -c 'source ./scripts/dev-open.sh && exec $SHELL'"
  }
}
